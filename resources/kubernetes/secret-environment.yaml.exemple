apiVersion: v1
kind: Secret
metadata:
  name: environment-secret
type: Opaque
data:
  ADMIN_PASSWORD: <MSR admin password, encoded in base64>
  DB_PASSWORD: <Database password, encoded in base64>
  SMTP_AUTH_PASSWORD: <SMTP password, encoded in base64>
